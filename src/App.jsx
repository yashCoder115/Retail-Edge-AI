// This file intentionally re-exports the canonical dashboard component from
// retailedge-ai.jsx at the project root, so that file remains the single
// source of truth (and the same file you can drop into a Claude Artifact)
// instead of maintaining two copies that could drift apart.
export { default } from "../retailedge-ai.jsx";
